export enum Paths {
  ROOT = "/",
  HOME = "/home",
  CREATE_IDENTITY = "/create-identity",
  LOGIN = "/login",
  ONBOARDING = "/onboarding",
  REQUESTS = "/requests",
  SETTINGS = "/settings",
  DOWNLOAD_BACKUP = "/download-backup",
}
