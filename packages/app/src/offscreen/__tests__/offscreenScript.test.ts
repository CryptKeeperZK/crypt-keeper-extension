// import log from "log"; // Adjust the import path accordingly
// import { isDebugMode } from 'path/to/utils'; // Adjust the import path accordingly
// import { browser, Runtime } from 'path/to/browser'; // Adjust the import path accordingly
// import { offscreenMessageListener } from 'path/to/offscreen'; // Adjust the import path accordingly

// jest.mock("log", () => ({
//     default: {
//         setDefaultLevel: jest.fn(),
//         debug: jest.fn(),
//     },
// }));

// jest.mock('path/to/utils', () => ({
//     isDebugMode: jest.fn().mockReturnValue(true), // Adjust the return value as needed
// }));
// jest.mock()

// describe("offscreen/offscreenScript", () => {
//   test("should be able to listen to messages", () => {});
// });
